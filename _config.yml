defaults:
  - scope:
      path: "" # empty string means all files in the project
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

baseurl: ""
